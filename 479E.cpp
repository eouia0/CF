#include<cstdio>
#define mod 1000000007
using namespace std;
int n, a, b, k, i, j, d[50001], p[50001], temp

int main(){
	scanf("%d%d%d%d", &n, &a, &b, &k);
	
	for(i = 1; i <= k; i++){
		temp = abs(i-b);
		for(j = max(1, i-temp+1); j<min(i+temp, n); j++){
			if(j != i){
				
			}
		}
	}
	
	for(i = 1; i <= k; i++){
		for(j = 1; j <= n; j++){
			
		}
	}
}